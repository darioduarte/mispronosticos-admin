# Copia estas variables en Vercel → Project → Settings → Environment Variables
# (Production, Preview y Development según corresponda)

# URL pública del backend Express en DigitalOcean (SIN barra final)
NEXT_PUBLIC_API_BASE_URL=https://mispronosticos.com

# Opcional — login con Google (mismo ID que GOOGLE_ADMIN_CLIENT_ID en el backend)
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
